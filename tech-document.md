# 视觉端 ↔ 上位机 接口契约 v1.1

> **签字确认**：本文档定稿后由视觉识别负责人和上位机负责人各签一份，作为联调阶段的唯一对齐基准。任何字段/语义调整必须 bump 协议版本并重签。

| 项 | 值 |
|---|---|
| 协议版本 | **1.1** |
| 生效日期 | 2026-05-10 |
| 视觉硬件 | Intel RealSense D435i（双目 + IMU） |
| 视觉算法 | YOLO（具体版本由视觉端决定，上位机不感知） |
| 上一版本 | 1.0（单目，2D 坐标） |

---

## 1. 责任边界（最重要，先看这个）

| 任务 | 视觉端 | 上位机 |
|---|---|---|
| D435i 驱动、彩色/深度对齐 | ✅ | ❌ |
| YOLO 推理、bbox + class + conf | ✅ | ❌ |
| 像素 → 相机坐标系 3D 反投影（用 D435i 内参） | ✅ | ❌ |
| 相机坐标系 → **皮带坐标系** 外参变换 | ✅（读上位机交付的 `calibration.yaml`） | ❌ |
| 跨帧目标跟踪（同一矸石不重复发） | ❌ | ✅（`BeltTracker`） |
| 皮带坐标系 → 机械臂坐标系 | ❌ | ✅（`coord_transform.py`） |
| 抓取决策、状态机、统计、录制 | ❌ | ✅ |
| 标定向导（采集 ChArUco 标定板，算 R\|t） | 协助现场标定 | ✅（`tools/calibration_wizard.py`） |

**核心原则**：
1. 视觉端**只输出皮带坐标系的 3D 点**，不要关心机械臂在哪。
2. 视觉端**不做跨帧跟踪**。每帧 `objects[].id` 仅在当前帧内唯一即可，跨帧重新分配也没关系——上位机的 `BeltTracker` 用 XY 距离 + 皮带速度自己复算 track_id。
3. 上位机**不做相机内参标定**——D435i 出厂自带，视觉端用 `pyrealsense2` 直接拿。

---

## 2. 网络层

| 项 | 值 |
|---|---|
| 协议 | **TCP**，长连接 |
| 角色 | 视觉端 = **Server**（监听）；上位机 = **Client**（主动连） |
| 默认端口 | **9001**（在 `config/default.yaml` 的 `vision.port`） |
| 编码 | **UTF-8** |
| 帧分隔 | **`\n`**（换行符）。每行一个完整 JSON 对象（NDJSON / JSON Lines） |
| 心跳 | 视觉端每 1 秒主动推一帧（哪怕 `objects=[]`），上位机超时 3 秒认为掉线 |
| 重连 | 上位机自动重连，间隔 1 秒（已在 `vision_client.py` 实现） |
| 视频流 | **不在本协议中**。如需推 RGB 帧，另开 RTSP 信道（建议 8554 端口），见 §6 |

---

## 3. 协议契约：`VisionResult`（视觉端 → 上位机）

### 3.1 完整 JSON 示例

```json
{
  "protocol_version": "1.1",
  "frame_id": 1024,
  "timestamp": "2026-05-10 18:00:00.120",
  "image_width": 1280,
  "image_height": 720,
  "belt_speed_mm_s": 180.0,
  "objects": [
    {
      "id": 1,
      "class_id": 1,
      "class_name": "gangue",
      "confidence": 0.86,
      "bbox": [320, 180, 80, 60],
      "center_pixel": [360, 210],
      "world_coord_mm": [125.4, 68.2, 42.5],
      "depth_valid": true,
      "sharpness_score": 0.78,
      "send_to_robot": true
    }
  ]
}
```

### 3.2 顶层字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `protocol_version` | string | ✅ | 必须为 `"1.1"`，不一致上位机直接丢弃帧 |
| `frame_id` | int (≥0) | ✅ | 全局递增，断线重连后从 0 重新计数也允许 |
| `timestamp` | string | ✅ | **本机时间** `YYYY-MM-DD HH:MM:SS.fff`（毫秒精度）。建议视觉端做 NTP 对时 |
| `image_width` | int (≥1) | ✅ | 彩色帧分辨率 |
| `image_height` | int (≥1) | ✅ | |
| `belt_speed_mm_s` | float (≥0) | ✅ | 皮带速度。视觉端可暂时硬编码 180.0；后续接 PLC 后由 PLC 提供给视觉端 |
| `objects` | array | ✅ | 当前帧检测到的目标列表，**可以为空** `[]` |

### 3.3 `objects[]` 字段

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `id` | int | ✅ | **当前帧内唯一**即可，不要求跨帧一致 |
| `class_id` | int | ✅ | `0=coal`, `1=gangue` |
| `class_name` | string | ✅ | 必须严格为 `"coal"` 或 `"gangue"`，其他值上位机直接拒绝整帧 |
| `confidence` | float [0,1] | ✅ | YOLO 输出的置信度 |
| `bbox` | [x,y,w,h] | ✅ | **像素坐标**，左上角原点。x、y 是左上角，w、h 是宽高 |
| `center_pixel` | [x,y] | ✅ | bbox 中心像素坐标。视觉端可直接 `[x+w/2, y+h/2]` |
| `world_coord_mm` | **[x,y,z]** | ✅ | **皮带坐标系，单位 mm**。详见 §4 坐标系约定 |
| `depth_valid` | bool | ⚠️ 强烈建议 | 默认 true。深度无效时置 false，**z 仍要给一个数**（用 0.0），上位机会自动退化为皮带平面抓取 |
| `sharpness_score` | float [0,1] | ✅ | 图像清晰度评分（用于过滤运动模糊帧）。视觉端可暂用固定 0.8 |
| `send_to_robot` | bool | ✅ | 视觉端的"是否建议下发"提示。**最终是否下发仍由上位机决定**（要过 `BeltTracker` 去重和状态机守卫） |

### 3.4 严格校验规则（pydantic `extra="forbid"`）

- 任何**未列出的字段**都会被上位机直接拒绝整帧（`ValidationError`）。
- 任何**类型不匹配**（如 `class_name: "rock"`）也直接拒绝。
- 视觉端如需新增字段，必须 bump 协议版本，**禁止偷偷加字段**——上位机不会忽略未知字段。

---

## 4. 坐标系约定（最容易扯皮，必须钉死）

### 4.1 皮带坐标系（视觉端输出）

```
        +Y (横向)
         ▲
         │
         │
   ──────┼───────► +X (皮带流动方向)
         │
         │
         O (原点：约定为相机正下方在皮带平面的投影点)
         │
         ▼
        +Z 向下？  ❌  本协议规定 +Z 向上（垂直于皮带平面，远离皮带）
```

| 项 | 约定 |
|---|---|
| 原点 | **相机正下方在皮带平面的投影点**（标定时由 `tools/calibration_wizard.py` 锚定） |
| +X 轴 | **皮带流动方向**（矸石被运输的方向） |
| +Y 轴 | **横向**，从皮带左边缘指向右边缘（站在皮带尾部往前看） |
| +Z 轴 | **向上**（远离皮带平面，矸石顶面 z>0；皮带平面本身 z=0） |
| 单位 | **毫米（mm）**，浮点数即可，精度建议 0.1 mm |

### 4.2 z 字段的语义

`world_coord_mm[2]` 是**矸石顶面**距皮带平面的高度，用于：
- 机械臂吸盘下压时找到正确的接触面，避免空抓或撞击
- 评估矸石厚度（影响夹爪选择/吸力）

**典型范围**：煤块 15-45 mm，矸石 30-80 mm。视觉端可以从 D435i 深度图取 bbox 内最高点（最小深度值）反算。

### 4.3 D435i 内参/外参

- **内参**（fx, fy, cx, cy, 畸变系数）：D435i 出厂标定，视觉端通过 `pyrealsense2` 的 `get_intrinsics()` 直接拿，**不写入 yaml**。
- **外参**（相机 → 皮带的 R\|t）：现场标定后由上位机写入 `config/calibration.yaml`，视觉端启动时读取。**这是唯一需要协调的标定文件**。

`config/calibration.yaml` 字段（视觉端按此读取）：

```yaml
vision:
  camera_to_belt:
    R: [[r11, r12, r13], [r21, r22, r23], [r31, r32, r33]]   # 3x3 旋转矩阵
    t: [tx, ty, tz]                                           # 平移向量 mm
```

---

## 5. 时间戳与时序

| 场景 | 约定 |
|---|---|
| `timestamp` 来源 | 视觉端**采集时刻**（拍照时刻），不是推送时刻 |
| 时区 | 本机时区即可（不强制 UTC），但上下位机要在同一台机/同一局域网保持时钟同步 |
| 精度 | 毫秒。格式 `YYYY-MM-DD HH:MM:SS.fff` |
| 推送频率 | 推荐 5-15 fps（与 YOLO 推理速度匹配）。**空帧也要推**（保心跳） |
| 顺序保证 | TCP 天然顺序，视觉端**不要并发推送**（多线程也要串行写 socket） |

---

## 6. 视频流（独立信道，可选）

JSON 信道**绝不允许**塞图像（base64 / bytes 都不行）。如需在上位机界面显示彩色帧+识别框：

| 阶段 | 方式 | 端口 | 说明 |
|---|---|---|---|
| **联调期** | 上位机走演示模式（JSONL 回放 + Mock 视频） | — | 不依赖视觉端视频流，先打通逻辑 |
| **现场试运行** | 视觉端开 RTSP 推流彩色帧 | 8554 | 推荐 H.264，1280x720@10fps，码率 2-4 Mbps |
| **可选增强** | 在 H.264 SEI 自定义元数据携带 `frame_id` | — | 用于 bbox 与视频帧严格对齐；如果不做，上位机按 timestamp ±50ms 近邻匹配 |

上位机 `video_panel.py` 用 `cv2.VideoCapture("rtsp://...")` 拉流。

---

## 7. 错误处理

### 7.1 视觉端侧

| 异常 | 处理 |
|---|---|
| 相机断开 | 推送 `objects=[]` 并继续重试连相机；不要主动断 TCP |
| YOLO 推理失败 | 推送 `objects=[]`，不要崩溃 |
| 深度对齐失败 | 该 object 的 `depth_valid=false`，z 给 0.0 |

### 7.2 上位机侧

| 异常 | 处理 |
|---|---|
| `protocol_version` 不一致 | 丢弃帧 + `logger.error`，不断 TCP（避免风暴重连） |
| `ValidationError`（字段不合法） | 丢弃帧 + `logger.error(frame_id=..., reason=...)`，不断 TCP |
| 3 秒未收到任何帧 | Heartbeat 超时 → 状态机切 `PAUSED`（待 `Heartbeat` 接入主流程） |
| `confidence < threshold`（在 `default.yaml` 配置） | 不下发 `PickCommand`，但仍记录到日志 |

---

## 8. 联调路线图（按此顺序推进）

| 阶段 | 谁参与 | 交付标准 |
|---|---|---|
| **0. 协议签字** | 视觉端 + 上位机 | 双方在本文 §3 字段表上签字 |
| **1. Mock 联调** | 上位机独立 | 上位机用 `tools/mock_vision_server.py` 跑通到 `PickCommand` |
| **2. 视觉端 JSON 自测** | 视觉端独立 | 视觉端能给出符合本协议的 NDJSON（用 `python tools/check_vision_json.py <file>` 校验，**待补**） |
| **3. JSON 联调** | 双方各启一台 | 视觉端推真 JSON，上位机演示模式视频 + 真识别框 |
| **4. RTSP 联调** | 双方 | 上位机 video_panel 拉 RTSP，bbox 严格对齐 |
| **5. 标定** | 双方 + 机械结构 | 跑标定向导，calibration.yaml 落地，验证 z 精度 ±5 mm |
| **6. 全链路 Demo** | 全员 | 真识别 + 真机械臂，至少 1 分钟无 FAIL 闭环 |

---

## 9. 变更历史

| 版本 | 日期 | 变更 |
|---|---|---|
| 1.1 | 2026-05-10 | • 接入 D435i 双目深度相机<br>• `world_coord_mm` 由 2D `[x,y]` 升级为 3D `[x,y,z]`（z 为矸石顶面高度）<br>• 新增 `depth_valid` 字段标记深度可信度<br>• 明确皮带坐标系约定（+X 流动方向，+Z 向上） |
| 1.0 | 2026-05-08 | 初版（单目 + YOLO，2D 坐标） |

---

## 10. FAQ

**Q1：视觉端能不能直接给相机坐标系下的点？**
A：**不行**。如果视觉端给相机系，那么 `calibration.yaml` 的 R\|t 就要在上位机重复用一次（每帧每目标），违反"协议字段语义清晰"原则。统一约定为皮带坐标系。

**Q2：z 我算不准（D435i 在 1m 距离的深度精度约 ±2%），怎么办？**
A：能给就给，给不准就把 `depth_valid` 置 false，上位机会自动退化为 z=0（皮带平面抓取）。**绝对不要瞎给一个 50.0 来糊弄**——机械臂下压会撞坏。

**Q3：YOLO 偶尔把传送带上的螺丝识别成 gangue 怎么办？**
A：这是模型问题不是协议问题。短期靠 `confidence` 阈值过滤；长期靠 `sharpness_score` + 上位机做置信度漂移检测（评审 P2 项）。

**Q4：如果 D435i 出问题暂时没有深度，上位机能否退化运行？**
A：**能**。所有 object 都给 `depth_valid: false` + `z: 0.0`，上位机降级为"按皮带平面抓取"，机械臂走预设的 Z 轨迹即可。功能不丢但抓取成功率会下降。

**Q5：上位机会不会把识别结果再回推给视觉端？**
A：**不会**。本协议是单向的（视觉 → 上位机）。视觉端不需要监听任何上位机输出。

---

**最后**：本文档每次更新都要在 §9 加一行，并同步修改 `src/aimcoal/core/models.py` 的 `PROTOCOL_VERSION` 常量、`config/schemas/vision_result.schema.json`、`tests/fixtures/sample_recording.jsonl`、`tools/mock_vision_server.py`——四处必须一致。
